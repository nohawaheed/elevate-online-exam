import { PercentageFormatPipe } from './percentage-format.pipe';

describe('PercentageFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
